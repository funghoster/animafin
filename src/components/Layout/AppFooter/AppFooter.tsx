import { Layout } from "antd";
import style from "./AppFooter.module.scss";

const AppFooter = () => {
  return <Layout.Footer className={style.footer}>Footer</Layout.Footer>;
};

export default AppFooter;
