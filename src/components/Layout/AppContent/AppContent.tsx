import { Layout } from "antd";
import style from "./AppContent.module.scss";

const AppContent = () => {
  return <Layout.Content className={style.content}>Content</Layout.Content>;
};

export default AppContent;
